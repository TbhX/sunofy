/** @type {import('next').NextConfig} */
const nextConfig = {
  // Cloudflare Pages compatibility
  // (Optional: add any needed custom config here)
};

export default nextConfig;

export default nextConfig;
